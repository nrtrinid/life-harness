/** Matches default `SCOUT_MAX_INPUT_CHARS` on ai-gateway (Ask/Chat Harness). */
export const DEFAULT_GATEWAY_MAX_INPUT_CHARS = 12_000;

/** Recommended Raw Lab input budget when `SCOUT_RAW_LAB_MAX_INPUT_CHARS` is unset on gateway. */
export const DEFAULT_RAW_LAB_MAX_INPUT_CHARS = 18_000;
